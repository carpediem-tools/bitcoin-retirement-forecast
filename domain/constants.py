"""Bear integrity constants (pure business — never user-tunable).

Intentionally EMPTY in this scaffold. The Bear constants (POWER_LAW_EXPONENT,
PLATEAU_ARR, SIGMOID_CALENDAR_ORIGIN, etc.) are added in a dedicated brief.
They live here — not in ``config/`` — to keep ``domain/`` self-contained and
the non-regression oracle isolated (CLAUDE.md "Frontière config/ vs domain/").
"""
